<!DOCTYPE html>

<html>
    <head>
        <title>TODO supply a title</title>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
    </head>
    <body>
      <?php
      ini_set('display_errors', 1);
      ini_set('display_startup_errors', 1);
      error_reporting(E_ALL);
      
      // abre el archivo y lo lee hasta lacantidad bytes indicada //
   
       // $miArchivo = fopen("mi_archivo.txt", "r");
       // $contenido = fread($miArchivo, filesize("mi_archivo.txt"));
       // echo $contenido;
       // fclose($miArchivo);
        
        // lee una linea ddel archivvo//
       // $miArchivo = fopen("mi_archivo.txt", "r");
       // $primeraLinea = fgets($miArchivo);
       // echo $primeraLinea;
       // fclose($miArchivo);
      
      
      // Escribir en un archivo (vacio.txt)
    
     // $miArchivo = fopen("vacio.txt", "w");
     // fputs($miArchivo, 'Federico Santos');
     // fclose($miArchivo);
      
      ?>
        
    </body>
</html>
